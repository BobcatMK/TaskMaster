$(".clear-for-ajax").empty();
$(".pop-up").remove();
$(".menu-for-ajax").empty();
$(".menu-for-ajax").append("<%= j(render :partial => 'layouts/for_users/menu_users') %>");
$("meta[name='csrf-token']").remove();
$("meta[name='csrf-param']").remove();
$("head").append("<meta name='csrf-token' content='<%= form_authenticity_token %>'>");
$("head").append("<meta name='csrf-param' content='<%= request_forgery_protection_token %>'>");
$(".clear-for-ajax").append("<%= j(render template: 'user/logged_signed',locals: {:@full_calendar => Calendar.all}) %>");